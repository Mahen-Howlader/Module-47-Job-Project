function Storedata({data}) {
    const {
        logo,
        job_title,
        location,
        remote_or_onsite,
        company_name,
        job_type,
        salary,
        experiences,
        id
      } = data;
    return (
        <div>
            
        </div>
    );
}

export default Storedata;